import { AppBar, IconButton, Toolbar, Typography } from "@mui/material"

export const NavBar = () => {
  return (
    <AppBar position="fixed" elevation={ 0 }>
    <Toolbar>
          <Typography variant="h6">Prueba tecnica MIREX</Typography>
    </Toolbar>
</AppBar>
  )
}
